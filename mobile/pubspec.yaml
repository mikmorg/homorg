name: homorg_camera
description: Remote camera companion for Homorg stocker sessions.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  flutter_bluetooth_serial: ^0.4.0
  permission_handler: ^11.3.1
  mobile_scanner: ^5.2.3
  camera: ^0.11.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
